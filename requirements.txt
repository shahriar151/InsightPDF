# Binary dependency fix (install first)
pysqlite3-binary

# Core App Libraries
streamlit

# LangChain Modules (Pinned for stability)
langchain==0.2.1
langchain-groq==0.1.3
langchain-community==0.0.32
langchain-core==0.2.3

# Embedding dependencies
langchain-huggingface==0.0.3 
sentence-transformers

# Vector Store
chromadb==0.4.24

# Document Loader
pypdf

# Pydantic is a dependency of LangChain and will be pulled automatically
# Removing the explicit, strict pin to prevent version conflict errors.